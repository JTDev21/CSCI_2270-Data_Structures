// Pointers
    // Three data types in c++
        //  - Simple: integer, bool, float
        //  - Structed: class, struct, enumeration
        //  - Pointers
                // A Pointer is a variable whose content is an address (that is, a memory address)
// Pointers
    // Equivalent pointers
        // int *p;
        // int* p;
        // int * p;
    // Note:
        // int *p, q;
            // p is a pointer, but not q.
        //  int *p, *q;
            // p and q is a pointer.
// use (*) for pointers called "Dereference operator"
// (&) is called the address of operator

// // --------------------------------------------------------------
// #include <iostream>
// using namespace std;

// int main()
// {
//     int x = 1000;
//     int *p;
//     p = &x;

//     // cout << p << endl;
//     // cout << *p << endl;
//     return 0;
// }
// // --------------------------------------------------------------

// Pointers
    // If you don't want a pointer to point to anything, set it to NULL. Useful for avoiding tanglement
// "->" is referred to as an arrow operator and is used to dereferencing a pointer, so the syntax is the same as (*ptr).













// #include <iostream>

// int main()
// {
//     return 0;
// }