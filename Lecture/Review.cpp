// Use the fstream lib to read/write to a file
    // ifstream(to read a file)
    // ostream(to read to a file)

// Streams - Reading lines
    // getline() is use to get all lines from a file

// Structures
    // a structure is a collection of related data terms, posssiblly of different types


#include <iostream>

int main(int argc, char const* argv[])
{
    
    return 0;
}